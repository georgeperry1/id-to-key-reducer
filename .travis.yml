language: node_js
node_js:
  - "9"
install:
  - npm install
  - npm install -g codecov
script:
  - npm test && codecov
before_deploy:
  - git add .
  - git commit -m 'travis testing'
deploy:
  provider: npm
  on:
    branch: master
